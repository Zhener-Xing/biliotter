# BiliOtter 安装与使用指南

分发测试版，目前还存在一些问题，安装略为繁琐，按步骤做就行。

---

## BiliOtter 是什么？

BiliOtter（桌面小水獭）是一个 **B 站学习助手**：

- 在 B 站看视频时，小獭陪你专注学习  
- 帮你记笔记、建课程组、画思维导图  
- 可以聊天提问、出题自测、加好友摸獭  

**需要两样东西一起用：**

1. **桌面宠物**（本机运行的小程序）  
2. **浏览器扩展**（装在 Chrome 或 Edge 里，连着 B 站）

缺一不可：只开桌宠不开扩展，知识库会锁住；只装扩展不开桌宠，也连不上。

---

## 一、安装桌宠

这就是你获得的安装包，里面有这个安装说明和你需要的一切。

### Mac 电脑

1. 找到 **`BiliOtter.app`**（可能在压缩包里，先双击解压）。  
2. **第一次打开**：不要直接双击，请 **右键 → 打开**，再在弹窗里点「打开」。  
   - 若被系统拦截：打开「系统设置 → 隐私与安全性」，点「仍要打开」。  
3. 屏幕上出现像素风小水獭，就说明装好了。

### Windows 电脑

1. 解压 **`BiliOtter-win32-x64.zip`**（或类似名字的 zip）。  
2. 进入解压后的文件夹，双击 **`BiliOtter.exe`**。  
3. 若弹出「Windows 已保护你的电脑」：点「更多信息」→「仍要运行」。  
4. 看到小水獭在桌面上，就成功了。

> 不用改什么配置文件。AI 能力会走官方云端，一般不用自己填 API Key。

---

## 二、安装浏览器扩展（很重要）

请用 **Chrome** 或 **Edge**。

### 1. 准备扩展文件夹

任选一种：

- 单独发给你的 **`bili-pet-bridge.zip`**：解压到任意位置（例如桌面上的 `bili-pet-bridge` 文件夹）  
- 或在桌宠安装目录里找：  
  - **Mac**：`BiliOtter.app` 右键「显示包内容」→ `Contents/Resources/bili-pet-bridge`  
  - **Windows**：安装目录下的 `resources/bili-pet-bridge`

记住这个文件夹的位置，下一步要用。

### 2. 加载到浏览器

**Chrome：**

1. 地址栏输入：`chrome://extensions` 回车  
2. 打开右上角 **「开发者模式」**  
3. 点 **「加载已解压的扩展程序」**  
4. 选中上一步的 **`bili-pet-bridge` 文件夹**（选文件夹本身，不要选里面的某个文件）

**Edge：**

1. 地址栏输入：`edge://extensions` 回车  
2. 同样打开 **开发者模式** → **加载解压缩的扩展** → 选同一个文件夹  

扩展列表里出现 BiliOtter / bili-pet 相关扩展，并保持 **已启用**。

### 3. 登录 B 站

1. 打开 [www.bilibili.com](https://www.bilibili.com)  
2. **登录你的 B 站账号**  
3. 保持这个浏览器开着（至少扩展处于启用状态）

桌宠连上后，一般会提示数据就绪 / 拉取完成。若提示「插件未在线」，请检查扩展是否启用、B 站是否已登录。

---

## 三、日常怎么用

### 和小獭互动

| 操作 | 作用 |
|------|------|
| **单击**小獭 | 打开 **当前正在看的视频** 的笔记 |
| **双击**小獭 | 打开 **知识库**（所有笔记、课程组） |
| **右键**小獭 | 打开 **好友**（加好友、摸獭、收笔记） |

看 B 站学习视频时，尽量让小獭在前台、扩展保持开启，它才能帮你记专注、同步账号。

### 记笔记

1. 打开 B 站视频 → 单击小獭打开笔记窗  
2. 自己写要点；可拖入截图  
3. 需要时点 **「一键整理」**：AI 会在文末补充整理，不乱改你原来写的内容  
4. 内容会自动保存进知识库（按你的 B 站账号分开存放）

### 知识库

双击小獭打开：

- **笔记**：搜索、预览、编辑、删除；可分享给好友  
- **课程组**：把相关视频收成一组；可建文件夹；可 **一键生成思维导图**  
- **学习活跃度**：看最近学了多久（热力图）

### 聊天（问小獭）

快捷键（先按第一个，很快再按第二个）：

| 系统 | 打开聊天 |
|------|----------|
| Mac | `⌘XO` |
| Windows | `Ctrl+X+O` |

可以问最近学了什么、让它根据笔记回答问题等。  
和知识库有关的问题，它主要根据你的笔记来答，不会瞎编。

### 学习计划 `/plan`

在聊天里输入，例如：

```text
/plan 我想学人工智能
```

小獭会给出课程体系预览；你可以改名字、改模块，确认后再创建课程组和文件夹。  
若与已有课程组太像，它会先问你要不要换名。

### 答题自测 `/game`

在聊天里输入：

```text
/game
```

或：

```text
/game 考一下线性代数
```

按提示选范围（课程组 / 文件夹 / 当前视频 / 主题），开始答题。  

- 中途退出：Mac `⌘S` 再 `⌘G`；Windows `Ctrl+S` 再 `Ctrl+G`  
- 笔记够多之后，题目可能已经预先生好，开局会更快

### 稍后再看 / 收藏

在聊天里可以说「加入稍后再看」「加入收藏」，或用：

- `/watchlater`  
- `/fav`

（需已登录 B 站，且扩展在线。）

### 好友与摸獭

1. 右键小獭打开好友窗  
2. 一方生成 **4 位数字密钥**（限时有效）  
3. 另一方输入同一密钥，即可互加好友  
4. 可以摸对方的獭、互传笔记  

---

## 四、推荐使用顺序（第一次）

1. 打开 **桌宠**  
2. 装好并启用 **浏览器扩展**  
3. 浏览器打开 B 站并 **登录**  
4. 等小獭提示数据就绪（或「数据拉取完成」）  
5. 随便打开一个学习视频，**单击小獭**试写笔记  
6. **双击小獭**进知识库，确认笔记在列表里  
7. 用聊天快捷键试一句「我最近学了什么？」

---

## 五、常见问题

**1. 小獭说插件未在线 / 知识库锁住了**  
- 扩展是否启用？  
- B 站是否已登录？  
- 是否用的是加载扩展的那个浏览器？  
试着刷新 B 站页面，或重新打开桌宠。

**2. Mac 打不开应用**  
右键「打开」，或到「隐私与安全性」里允许。

**3. Windows 被 SmartScreen 拦**  
「更多信息」→「仍要运行」。

**4. 聊天 / 出题失败、提示连不上 AI**  
先确认：桌宠已开、已登录 B 站、网络正常。  
若只有你一个人出现问题，把完整报错发给发放安装包的人。

**5. 换了一个 B 站号，笔记怎么变了？**  
这是正常的：每个 B 站账号有一份自己的知识库，避免串号。切回原来的号即可看到原来的笔记。

**6. 扩展升级后没了？**  
重新按「二、安装浏览器扩展」加载一次新的 `bili-pet-bridge` 文件夹即可。

**7. 想退出答题**  
Mac：`⌘SG`；Windows：`Ctrl+S+G`。

---

## 六、隐私小提示（了解即可）

- 笔记按你的 **B 站账号**分开存放在本机,同时云端存在备份，换设备需要从云端拉取，很慢，因此内测版建议只在一台设备上运行。  
- 聊天、出题等 AI 功能会经过官方配置的云端服务处理（你不需要自己填 API Key），如果你想填写自己的api-key，可以在example.env中进行操作填写。  
- 请保护好自己的电脑开机密码和 B 站登录状态。

---

## 七、给发放安装包的人（可选一页）

打包命令（在开发机仓库根目录）：

```bash
npm run pack
```

会得到两个 zip，发给对应系统的用户即可（**包内已含指南与扩展**）：

| 文件 | 发给谁 |
|------|--------|
| `dist/BiliOtter-macOS-*.zip` | Mac |
| `dist/BiliOtter-Windows-x64.zip` | Windows |

并确认自己的云端 API 已启动且健康检查正常，否则用户端 AI 功能会失败。

---

有问题先对照「五、常见问题」。仍解决不了，找到github上biliotter的仓库：https://github.com/Zhener-Xing/biliotter，直接pull request即可，发布者为大学生，更新可能慢，但看到一定会修的。
